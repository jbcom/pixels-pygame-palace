{
  "name": "@pixels/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python app.py",
    "start": "gunicorn app:app -k eventlet -w 1 -b 0.0.0.0:5001 --timeout 120",
    "test": "pytest",
    "lint": "mypy .",
    "clean": "rm -rf __pycache__ .pytest_cache .mypy_cache"
  }
}